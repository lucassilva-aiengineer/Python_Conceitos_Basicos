# Funções lambda

# Funções lambda, são  funções anônimas
# onde específicamos apenas a palavra chave os argumentos e 
# a expressão, no final é retornado apenas o resultado espressão.  

lambda numero: numero * 2 


multiplicacao = lambda a, b : a * b 

print(multiplicacao(10, 5))
